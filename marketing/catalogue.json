{
  "$comment": "Platform capability catalogue - constitution IV: every configured platform credential/destination is enumerable here, and check.js fails CI when a row, adapter, or registry entry is missing its counterparts. The publisher publishes in array order (wordpress first: social copy needs the post URL).",
  "platforms": [
    {
      "id": "wordpress",
      "kind": "adapter",
      "adapter": "adapters/wordpress",
      "role": "primary",
      "formats": {
        "media": [
          "image/png",
          "image/jpeg"
        ],
        "body": "html"
      },
      "schedulingModel": "at-time",
      "authModel": "basic-app-password",
      "credentialEnv": [
        "WP_APP_PASSWORD"
      ],
      "configEnv": [
        "WP_BASE_URL",
        "WP_USERNAME"
      ],
      "enabled": true
    },
    {
      "id": "mastodon",
      "kind": "adapter",
      "adapter": "adapters/mastodon",
      "role": "social",
      "formats": {
        "media": [
          "image/png",
          "image/jpeg"
        ],
        "maxChars": 500
      },
      "copySections": [
        "mastodon",
        "x"
      ],
      "schedulingModel": "at-time",
      "authModel": "bearer-token",
      "credentialEnv": [
        "MASTODON_TOKEN"
      ],
      "configEnv": [
        "MASTODON_BASE_URL"
      ],
      "enabled": true
    },
    {
      "id": "bluesky",
      "kind": "adapter",
      "adapter": "adapters/bluesky",
      "role": "social",
      "formats": {
        "media": [
          "image/png",
          "image/jpeg"
        ],
        "maxChars": 300
      },
      "copySections": [
        "bluesky",
        "x"
      ],
      "schedulingModel": "at-time",
      "authModel": "app-password-session",
      "credentialEnv": [
        "BSKY_APP_PASSWORD"
      ],
      "configEnv": [
        "BSKY_SERVICE",
        "BSKY_IDENTIFIER"
      ],
      "enabled": true
    },
    {
      "id": "linkedin",
      "kind": "delegated",
      "delegatedVia": "wordpress",
      "mechanism": "wp-linkedin-auto-publish",
      "$comment": "No adapter and no credential on purpose (PLAN.md 1.1): the site's WP LinkedIn Auto Publish plugin (verified installed + authorized 2026-09-12; Jetpack Social is NOT installed — the `jetpack/v4` namespace is WooCommerce's connection package) fans out the WP publish on transition_post_status. The publisher records state 'delegated', never a fabricated ok. Read-back = the plugin's `_sent_to_linkedin` post meta (Phase-1 verify item).",
      "credentialEnv": [],
      "configEnv": [],
      "enabled": true
    },
    {
      "id": "x",
      "kind": "adapter",
      "adapter": null,
      "role": "social",
      "$comment": "OMITTED for now (decision 2026-09-12). The site's twitter-auto-publish plugin would auto-tweet every WP publish (default-on for new posts); the chippr-marketing-rails must-use plugin (infra/wordpress/mu-plugins/) unhooks it for posts authored by marketing-bot, so the pipeline never reaches X until this row is enabled with its own adapter + credential (issue #165).",
      "credentialEnv": [],
      "configEnv": [],
      "enabled": false
    },
    {
      "id": "instagram",
      "kind": "adapter",
      "adapter": null,
      "role": "social",
      "$comment": "Phase 2 (issue #165). JPEG-only media when it lands.",
      "credentialEnv": [],
      "configEnv": [],
      "enabled": false
    },
    {
      "id": "tiktok",
      "kind": "adapter",
      "adapter": null,
      "role": "social",
      "$comment": "Phase 3 (issue #166). Content Posting API audit required before launch.",
      "credentialEnv": [],
      "configEnv": [],
      "enabled": false
    }
  ]
}
