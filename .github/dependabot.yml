# Dependabot: new surfaces only — the legacy packages/ workspace is frozen
# (constitution V) and deliberately receives no dependency work.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
  # npm for the marketing workspace lands with its package.json (Phase 1):
  # - package-ecosystem: "npm"
  #   directory: "/marketing"
  #   schedule:
  #     interval: "weekly"
