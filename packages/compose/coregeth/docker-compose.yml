version: '3.8'

services:
  COREGETH:
    image: ${IMAGE}
    networks:
      - backbone
    ports: 
      - 8545:8545
    volumes:
      - type: bind 
        source: ${DATA}
        target: /root
    command: 
      ${GETH_FLAGS} 

networks:
  backbone:
    name: chipprbackbone
    external: true
