{
  "version": "1.0.0",
  "private": false,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "new-version": "lerna version --conventional-commits --yes && npm publish",
    "diff": "lerna diff"
    },
  "name": "chippr-bots",
  "devDependencies": {
      "lerna": "^3.19.0"
  }
}
