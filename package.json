{
  "version": "1.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "build" : "lerna run build",
    "new-version": "lerna version --conventional-commits --yes",
    "publish" : "lerna publish",
    "diff": "lerna diff"
  },
  "name": "chippr-bots",
  "devDependencies": {
    "lerna": "^6.6.1"
  }
}
